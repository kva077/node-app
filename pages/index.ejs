<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Node SS</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65"
      crossorigin="anonymous"
    />
  </head>
  <header>
    <nav
      style="
        background-color: black;
        color: azure;
        color: rgb(118, 185, 221);
        display: flex;
        text-align: center;
        font-weight: bold;
        margin-bottom: 2rem;
      "
    >
      <span style="left: 3rem"
        >Node
        <hr style="margin: 0" />
        Simple Server</span
      >
    </nav>
  </header>
  <body>
    <% if (created) { %>
    <div class="alert alert-success">Note has been created!</div>
    <% } %>
    <div class="container">
      <form action="/" method="post" class="mb-2">
        <div class="form-group mb-2 mt-2">
          <input type="text" class="form-control" name="title" required />
        </div>
        <button class="btn btn-primary green" type="submit">Create</button>
      </form>
      <% if(notes.length) { %>
      <div class="card">
        <ul class="list-group">
          <% for (let i = 0; i < notes.length; i++) { %>
          <li
            class="list-group-item d-flex justify-content-between align-items-center"
          >
            <%= notes[i].title %>
            <div>
              <button
                data-type="edit"
                data-id="<%=notes[i].id%>"
                class="btn btn-primary"
              >
                Редактировать</button
              ><button
                data-type="remove"
                data-id="<%=notes[i].id%>"
                class="btn btn-danger ms-2"
              >
                &times;
              </button>
            </div>
          </li>
          <% } %>
        </ul>
      </div>
      <% } else { %>
      <p>No notes!</p>
      <% } %>
    </div>
    <script src="/app.js"></script>
  </body>
  <footer>
    <div
      style="
        font-size: small;
        background-color: black;
        color: azure;
        position: fixed;
        left: 0;
        bottom: 0;
        width: 100%;
        display: flex;
        justify-content: center;
        text-align: center;
      "
    >
      <span>All rights received.</span>
    </div>
  </footer>
</html>
